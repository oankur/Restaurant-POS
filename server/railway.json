{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npx prisma db push && npx prisma db seed && node dist/index.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
